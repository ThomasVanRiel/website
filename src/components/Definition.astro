---
type Definition = {
  number: number;
  text: string;
  example?: string;
};

type Props = {
  term: string;
  partOfSpeech?: string;
  pronunciation?: string;
  definitions: Definition[];
};

const { term, partOfSpeech, pronunciation, definitions } = Astro.props;
---

<div class="definition-block my-6 border-l-2 border-neutral-300 dark:border-neutral-400 pl-4">
  <div class="mb-2">
    <span class="text-lg font-bold">{term}</span>
    {pronunciation && (
      <span class="ml-2 text-lg italic">{pronunciation}</span>
    )}
  </div>
  
  {partOfSpeech && (
    <div class="text-sm italic  mb-3">
      {partOfSpeech}
    </div>
  )}
  
  <ol class="space-y-2">
    {definitions.map((def) => (
      <li class="text-sm flex gap-2">
        <span class="font-semibold min-w-[1.5rem]">{def.number}.</span>
        <div class="flex-1">
          <span class="">{def.text}</span>
          {def.example && (
            <div class="mt-1 text-sm italic">
              "{def.example}"
            </div>
          )}
        </div>
      </li>
    ))}
  </ol>
</div>
