---
import ringRoadsData from "./ring_roads.json"
import RoadGrid from "../../../components/RoadGrid.astro"

const images = ringRoadsData.map((road) => {
  const city = road.ring_road[0]
  const roadId = road.ring_road[1]
  return {
    src: `/ring-roads/${roadId}.png`,
    alt: `${city} ${roadId} - Ring Road`,
    name: `${city} - ${roadId}`,
    filters: [{ closedFraction: road.closed_fraction }],
    sorters: [{ radius: road.radius, rmsError: road.rms_error_m, rmsErrorNorm: road.rms_error_norm, city: city, roadId: roadId }]
  }
})
---

<RoadGrid 
  images={images} 
  columns={4} 
  gap="md" 
/>