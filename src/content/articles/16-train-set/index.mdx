---
title: "The math of a good train track"
summary: 'Turning a kid''s question "is this a good train track?" into something we can actually measure, with a little graph theory and a random walk.'
date: "05/28/2026"
draft: true
tags:
  - Math
  - Interactive
---

import TrackEmbed from "@components/TrackEmbed.astro";
import ResearchQuestion from "@components/ResearchQuestion.astro";
import Scaffold from "@components/Scaffold.astro";

export const questions = [
  {
    number: 1,
    question: "Can the train reach everywhere without changing direction?",
    hypothesis:
      "A train going forward only may not be able to reach all parts of a layout.",
    answer:
      "SCAFFOLD: Short direct answer to Q1 — e.g. 'Only if the layout is strongly connected. One badly placed diagonal can cut off segments; a second one can restore access.'",
  },
  {
    number: 2,
    question: "Which part of the track sees the most traffic?",
    hypothesis:
      "The train drives more over certain pieces of track due to layout choices, especially switch arrangements.",
    answer:
      "SCAFFOLD: Short direct answer to Q2 — e.g. 'Switches are the great equalizer. More switches spread across the layout means more even traffic. The score figures show exactly where traffic concentrates.'",
  },
  {
    number: 3,
    question: "How do I build a better track?",
    hypothesis:
      "The answers to the first two questions translate directly into design rules any builder can apply.",
    answer:
      "SCAFFOLD: Short direct answer to Q3 — point to the quick tips below, or write a one-sentence summary.",
  },
];

Wooden train tracks like BRIO, IKEA Lillabo, or one of their many compatible cousins are part of the toys in most households.
Anyone who has snapped together a wooden train track has at some point stepped back to wonder whether the layout is any _good_.
There is a real instinct behind that question, as some tracks feel satisfying to run a train on, while others feel broken and need a lot of interaction.
While moving the train manually is fine, automatic trains that require a lot of interaction can get tiring, taking away attention from other parts of the playset, distributed in every other corner of the living room.
This article turns that instinct into something we can measure.

<TrackEmbed src="intro.json" initialSpeed={50} hideStats hideControls />

It's worth thinking of some questions we would like to answer to figure out if the train tracks are good, or actually measure the appeal of a track. Before answering them, we'll commit to a **hypothesis** for each — an educated guess to test our findings against.

{questions.map(({ answer, ...q }) => <ResearchQuestion {...q} />)}

To answer these questions, we need two things: a precise way to describe the track, and a way to evaluate it.

## Graph theory, the math behind train tracks

**Graph theory** is a field of mathematics which studies connections between points or **nodes**.
The mathematician Leonhard Euler used it in the 18th century to solve mathematical puzzles.
[He wondered if he could cross the river in Königsberg using the seven bridges, using every bridge exactly _once_.](https://www.youtube.com/watch?v=W18FDEA1jRQ)

### The track as a graph

When we look at our train track, we can see that it is a similar puzzle.
The points where the train has a **choice**, such as switches, are the nodes.
We can then simplify the track by combining all pieces in between two switches and calling them **edges**.
We have now simplified the many pieces of track into several nodes, connected by edges, which we call a **graph**.

You have probably seen this before, as the simplification is quite common in rail transport.
If we look at a **metro map** for instance, the stations are nodes where you have a choice (changing trains or hopping on or off), connected by a simple line.

We have a problem though, our switches are _not_ simple nodes. The train cannot go in every direction without reversing, which we have _forbidden_ in research question 1. Switches have a limited choice as the two branches are not connected to each other, only to the trunk.
This seemingly simple constraint complicates the graph immensely, as we no longer have a simple graph. Transit is **symmetric or undirected** as trains can drive in two directions by design. The choice in the nodes on the other hand is **asymmetric**, the train only has a choice when entering from the trunk.

<Scaffold>
Diagram needed (static, hand-drawn style — not an interactive embed): show a few track pieces
collapsing into a graph of nodes and edges, and a switch drawn with the branch->branch connection
crossed out, to make clear a switch is not an ordinary node — the train can pass trunk->branch but
never branch->branch.
</Scaffold>

A track is a collection of straight pieces, curves, and switches connecting them. Switches are the interesting part: they're the points where the train has a _choice_. Once we draw each segment as an edge and each junction as a node, the question becomes a question about a graph.

There's a subtlety the graph alone hides: every real piece — in BRIO, Lillabo, and all compatible systems — has a "male" end and a "female" end, so a valid track has to alternate M → F → M → F at every joint. Two male ends or two female ends bumping into each other simply won't connect. That's a _parity_ constraint sitting on top of the graph — and it's exactly what adapter pieces are for.

### Connectivity

Remember the directedness we talked about in the previous section? There is an important connection we can make to the research questions. The first question we pose contains the _directedness_: _"Can the train reach everywhere **without changing direction**?"_.

We can translate this into the graph as well. When we place the train on the track, we do not care about the initial direction. The train can go either forward or backward, but still should be able to reach every other part of track. In graph theory, we call this property **strong connectivity**.

<Scaffold>
Introduce weak vs. strong connectivity with a concrete example before naming the terms. IMPORTANT:
apply them to the oriented-segment graph from the previous subsection, NOT to the junction graph —
that distinction is the whole point.

- Weak connectivity: the physical layout is one connected piece — ignore direction and you can get
  from anywhere to anywhere. Almost always true; the easy condition.
- Strong connectivity: on the directed oriented-segment graph, you can reach every (segment,
  direction) state from every other by legal moves — forward only, no lifting the train off the
  track. Genuinely stronger because the switch turn-restrictions can trap a direction.

This is the formal answer to research question 1. Name the terms after the intuitive explanation,
not before. (The one-diagonal layout is the clean separator between weak and strong connectivity —
that walkthrough now lives in the "Adding one diagonal" section.)

</Scaffold>

A single-direction train cannot back up out of a dead end, so reaching every segment imposes a real constraint on the layout. The relevant graph property here is strong connectivity. And if we want to cover every _segment_ rather than just every junction, we are back to the same kind of puzzle Euler asked about the bridges of Königsberg: can you traverse every one of them exactly _once_?

## Simplifying the mathematics by simulation

Graph theory tells us about the structure of the track, but not how much traffic each segment gets. For that, we **simulate**. We will build a virtual model of the track that mirrors the train set, and use the computer to run the train at very high speeds, much faster than we can run it ourselves. We are making a **digital twin** of the track, an identical copy of the physical track, fully in the computer. The digital twin allows us to check the track really quickly, even before building it, and is what engineers use to test real infrastructure projects.

### Markov chains

We have already described the track as a graph, but not how it should be driven by the train. After every segment, the train can have a choice. Entering the trunk of the switch, the train chooses between the left or the right branch. The train carries no memory: where it goes next is decided entirely by the segment it's on right **now** — never by the route it took to get there. We call this a **Markov chain**.

The _strongly connected_ property of a graph transfers to Markov chains, where it is called **irreducible**. If a segment can never be reached it can be removed, _reducing_ the track.

Once we assign a probability to each switch side, every transition is known, and the whole track can be collected into a single **transition matrix**: one row per segment, listing the chance of moving to each next segment. The train's path becomes a random walk on a graph of segments. We can then calculate a score for every segment in a _closed form_ solution to know how much it is visited by the train. Busy segments score high; isolated tracks score low.

### Monte Carlo, the digital twin

To form a better understanding of the behaviour of the train on the track, we will use a more visual method.
By running a virtual train, we can see the path the train takes on the track. When the train arrives at a switch, it has a chance to take the left track, or to take the right track. We will assume an unbiased choice, the train takes the left track half of the time, the right track the other half.

It is also possible to assign other chances to the switch branches, maybe the left track is taken 80% of the time. We can even extend the choice to even more choices if we have a switch with more than two exits. Or if we need to roll two dice to determine the chance, like in board games such as Catan, or Monopoly.

The result of the simulation is not as exact as the closed form solution but can get close _enough_ after a while. It takes time to accumulate sufficient track traversals. The clearest example is right at the start: the train has only driven the first few pieces, so every other segment still shows zero traffic. Read too early, the simulation would tell us those segments are never visited — when really they just haven't had their turn yet. Further on in the simulation, some track segments are _difficult_ to reach and are not _yet_ driven. It is important to run the simulation long enough, while at the same time considering the amount of time available to run the simulation.

The score of each segment is calculated as the number of steps in which the segment was visited, divided by the total number of steps the train took.

We use Monte Carlo in this problem to gain a visual intuition in Markov chains and the Monte Carlo method, while an exact approach is available to score the tracks. Monte Carlo can also be applied in problems where an exact solution is not available. For weather forecasts for instance, meteorologists can run hundreds of slightly different simulations to generate forecasts. This is actually how we can know minimum and maximum temperatures, with an estimated temperature. This is why the estimates get less accurate the more in the future the forecast lies, more uncertainties have accumulated to skew the result.

In this article, the simulation is shown at a slow speed and might not give an accurate view of the Monte Carlo process. It is commonly run multiple times at once instead of only one train at a time to save a lot of time.

### How to read the figures

<Scaffold>
This subsection prepares the reader to interpret every figure in the Findings section. Write it
before the first figure appears — not after.

Key points to cover:

- Each segment has a bulge drawn alongside it, sized in proportion to its traffic score.
- The bulge is split into a left half and a right half — one per direction of travel.
- Symmetric bulge (equal halves): the train passes roughly equally in both directions.
- Lopsided bulge (one half much larger): traffic flows predominantly one way.
- Missing bulge on one side: that direction is never (or rarely) visited.

Stress that the figures are stochastic: each one is one run of the Monte Carlo simulation. The
reader should watch for a few seconds before drawing conclusions — the long-run pattern is what
matters, not the first few steps.

We will integrate statistics in this section instead of separate.

</Scaffold>

The natural way to surface these per-segment probabilities on the track itself is to draw a shape that grows alongside each segment in proportion to its traffic. Splitting that shape into a _left_ half and a _right_ half — one bulge per direction of travel — turns out to reveal more than the totals alone.

A _symmetric_ bulge means the segment is a two-way street: the train passes through it about as often in each direction. A _lopsided_ bulge means the segment is effectively one-directional — the layout funnels traffic through it in only one orientation. A plain oval shows up as 100% one-sided everywhere; a balloon track shows up as a symmetric trunk feeding a one-sided loop. So the visualisation isn't just an aesthetic display of the score — the symmetry pattern surfaces the directional structure of the layout for free.

<Scaffold>Add SVG renders of different bulge types.</Scaffold>

<Scaffold>
  The key statistic for a layout is **utilization**: what fraction of pieces are
  actually visited by the train in the long run? A piece that is never visited is
  dead weight — it takes up space on the floor but the train ignores it. A layout
  with 20 pieces where only 14 are visited has 70% utilization.

Each piece also has a directional sub-stat: is it used in both directions, or
only one? A piece used in both directions shows a symmetric bulge; a piece used
in one direction only shows a lopsided one. A piece with no bulge at all is not
utilized.

A plain oval is 100% utilized — every piece is visited — but entirely
one-directional: the train always goes the same way. A layout with switches can
be 100% utilized and bidirectional, meaning every piece is visited and in both
directions. That is the ideal.

**Stress that these statistics are stochastic and need time to settle.** The
simulation is a random walk, so the utilization and bidirectional percentages
are running estimates, not fixed readouts — they drift as the train runs and
are never truly final. Early on they read artificially high: the figure counts
every piece the train has touched so far, so a fresh run can sit at 100%
utilized before the long-run pattern emerges and idle segments drop out. Tell
the reader plainly to let each simulation run for a while before trusting the
numbers — the first few seconds are burn-in, not signal. They tend toward the
true value but approach it asymptotically.

</Scaffold>

## Putting the math to work with a virtual train

<Scaffold>
  Brief intro — one or two sentences. Something like: "Let's run the virtual
  train on a few layouts and see what falls out." We can relate this concept of
  simulating in conjunction with a real thing to a digital twin. It is not exact
  as the digital twin is not coupled to the real world, but it conveys the idea
  to the audience.
</Scaffold>

### A plain oval

<Scaffold>
The simplest closed loop.
- Strongly connected: yes. Every segment is reachable going forward.
- Traffic distribution: perfectly uniform — every segment scores identically.
- Score figures: 100% one-sided everywhere, reflecting that the train always goes the same way.
- RQ1: yes, the train reaches everywhere. RQ2: all segments equally.

Write 2-3 sentences interpreting what the reader sees. Point out the uniform lopsided bulges
explicitly so the reader knows what to look for in the next figure.

</Scaffold>

<TrackEmbed preset="oval" />

### Adding one diagonal

<Scaffold>
One interconnect cuts across the oval.
- Strongly connected: no. The diagonal segment is reachable going one way only.
- Score figures: the diagonal segment shows a heavily lopsided or absent bulge on one side.
- RQ1: no longer yes for all segments.

Point the reader to the specific segment that becomes unreachable and explain why in plain
language: the diagonal creates a shortcut that the train takes, bypassing the segment on
subsequent laps.

This is the clean SEPARATOR between weak and strong connectivity (moved here from the Connectivity
section): the track stays weakly connected — still one physical piece — but is NOT strongly
connected. The train crosses the diagonal once, the choice it made fixes its orientation, and that
state becomes unreachable forever. Two diagonals restore strong connectivity. (This is also the
dead-end-with-no-visible-stub finding.)

</Scaffold>

<TrackEmbed preset="oval-diagonal" />

If a diagonal interconnect is added to the track, that segment is reachable in one direction only. As soon as the train passed the segment, the direction is fixed and the diagonal segment can never be reached again.

<Scaffold>
  The diagonal connector is only ever crossed once — after that, the train is
  locked into a smaller loop and can never reach it again. This means a portion
  of the track sits permanently idle. Utilization drops below 100%: the diagonal
  itself and the outer bypass are never visited in the long run. The figures
  show this directly — those pieces have no bulge. Together they diagnose a
  layout with a hidden dead end: not a stub you can see, but a structural trap
  that reveals itself only once the train starts moving.
</Scaffold>

### Adding a second diagonal

<Scaffold>
The second interconnect restores balance.
- Strongly connected: yes again.
- Traffic distribution: even — the two diagonals give the train a way to travel in both directions.
- Score figures: bulges become more symmetric across all segments.
- Key insight: one switch can break reachability; a second, well-placed one can restore it.

Contrast explicitly with the previous figure.

</Scaffold>

<TrackEmbed src="TrackOval2Diagonal.json" />

Adding a second diagonal interconnect allows the train to change directions once again. Every segment of track gets visited the same amount, on average.

<Scaffold>
  Adding a second diagonal restores access to the whole layout — the train can
  now reach every section from every other section, so no track sits permanently
  idle. Utilization is back to 100%, and every piece now shows traffic in both
  directions. The contrast with the single-diagonal case is stark: one connector
  created a structural trap; two create a well-connected network. One piece of
  track made all the difference.
</Scaffold>

### A real track

<Scaffold>
PLACEHOLDER — replace with a deliberate layout that shows something the simpler ones don't: a
bottleneck, a quiet spur, or a particularly interesting asymmetry pattern. The current
TrackReal1.json was not chosen to illustrate a specific point.

Decide what finding to highlight before building the layout:

- A bottleneck (one segment with dramatically higher score than its neighbours)?
- A quiet spur (a dead-end branch that scores near zero)?
- An asymmetric junction (one branch of a switch far more used than the other)?

Write 3-4 sentences interpreting the figure once the layout is chosen.

</Scaffold>

<TrackEmbed src="TrackReal1.json" showAdapters advancedStats />

## Key takeaways

### Answers to our questions

<Scaffold>
  Fill in the hypothesis and answer text in the `questions` export at the top of
  this file. The component renders automatically from that data — no edits
  needed here.
</Scaffold>

{questions.map(q => <ResearchQuestion {...q} />)}

### Quick tips for a good track

<Scaffold>
4-5 short, actionable rules for builders. Suggested starting point (from the old Key takeaways):

- More switches -> more even coverage. Every switch is a decision point that mixes the train.
- Spread switches around. Two adjacent switches give the train only one meaningful choice.
- Watch out for switches whose branches rejoin quickly — you're paying for a switch and getting
  a straight piece.
- Avoid dead ends if you want one-direction running.
- A loop with a spur beats a long straight line.

The adapter tip is covered in the graph theory section — do not repeat it here.

</Scaffold>

## Build your own track

<Scaffold>
  Short intro — warm and inviting. Something like: "Everything above you can now
  explore yourself." One or two sentences, no more. Then embed the interactive
  editor. State the necessary simplifications to recreate the tracks in silico.
  Adapters, magic connectors. Implementing the loose connections is a hassle.
</Scaffold>

The interactive editor is live [on this webpage](https://brio.thomasvanriel.com/).

### Try it yourself

<Scaffold>
Three-beat structure:

1. Guided example — reproduce a finding from the article. E.g. "Build a plain oval and watch the
   score. Now add a single diagonal — what happens to that segment's score?"

2. Challenge — a concrete goal. E.g. "Can you build a layout where every segment gets equal
   traffic?" (Hint: it's harder than it looks on a layout with switches.)

3. Free play — open invitation. "Now build whatever you like and see what the
   score tells you." "Build a real track and create it's digital twin.
   What sections of your track should you alter to get a 'perfect' track?"

Keep the language light and inviting. This is the kid-on-the-floor moment.

</Scaffold>

## Future work

<Scaffold>
  Natural extensions to pick up in a follow-up article:

**Computing it exactly, deterministically.** DELIBERATELY CUT FROM THIS ARTICLE —
too heavy for a general audience; this article keeps Monte Carlo as the intuitive,
watchable answer and only gestures at "there's a closed form." The follow-up does
the real machinery: (1) build the oriented-segment line digraph (states = segment +
direction, edges = legal moves under the switch turn-restriction); (2) connectivity
is then an exact SCC decomposition (Tarjan/Kosaraju, O(V+E)) — strongly connected
iff one SCC; the condensation DAG separates the recurrent class from transient/dead
segments, so "which pieces are idle" is structural, not statistical; (3) the traffic
scores are the stationary distribution pi solving pi P = pi with sum(pi)=1 — a finite
linear solve (left eigenvector, eigenvalue 1), exact, no burn-in. Tie-off: Monte
Carlo is the deterministic target made watchable; Perron-Frobenius guarantees a
unique positive pi exactly when the chain is irreducible = strongly connected. This
is the rigorous backbone under everything the main article shows by simulation.

Formal vocabulary for the follow-up (kept out of the main body deliberately). The track is
really TWO graphs, and only one is a digraph:

- Physical track = an undirected MULTIGRAPH WITH FORBIDDEN TRANSITIONS (a "transition system").
  Undirected because segments are bidirectional — direction is NOT a property of the edges.
  Multigraph because parallel segments and self-loops are possible. Vertices = junctions/endpoints;
  edges = segments. Endpoints are degree-1 (pendant/leaf) vertices = the "must reverse" points;
  pass-through joints degree-2; switches degree-3. The switch constraint is exactly a "forbidden
  transition": only certain pairs of incident edges connect through the vertex. So the track is NOT
  a digraph — the constraint lives in the TRANSITIONS at the vertex, not on the edges.
- What the train walks / the math object = a genuine DIGRAPH. Build it by: (1) replace each segment
  with two antiparallel arcs (= the SYMMETRIC DIGRAPH of the track); (2) take its LINE DIGRAPH
  (vertices = oriented segments (segment, direction); arcs = legal moves) and prune the moves the
  transition system forbids. This "oriented-segment line digraph" is where digraph / strong
  connectivity / SCCs / the Markov chain properly live.
- Dynamics on it = a RANDOM WALK / MARKOV CHAIN on that digraph.

**Irreducibility.** The diagonal examples show that a layout can look connected
while hiding a structural trap — a segment reachable in one direction only,
which the train crosses once and never returns to. The formal property is
irreducibility: a chain is irreducible if every state is reachable from every
other state. An irreducible layout wastes no track. The interesting question is
then: what is the minimal set of pieces you would need to add (or reroute) to
make a given layout irreducible? That is a graph theory problem with a concrete,
hands-on answer.

**Richer traffic statistics.** Utilization tells you which pieces are used, but
not how unevenly the traffic is distributed among the ones that are. Two layouts
can both be 100% utilized while one has one wildly overloaded junction and
another has perfectly even traffic. A follow-up could introduce entropy (how
spread out is the distribution?) and the Gini coefficient (borrowed from
economics, where it measures income inequality — here it measures traffic
inequality) as complementary lenses on the same question.

**Factorio belt balancers.** Factorio players already have strong intuition for
"4-to-4 balanced" vs. "throughput unlimited" — two distinctions that map
directly onto strong connectivity and uniform stationary distribution. Belt
splitters have a parity-like constraint (inputs must equal outputs for a true
balancer), and the hidden bottleneck in a naïve layout is exactly the
one-diagonal trap from above. The stationary distribution framing applies
unchanged; only the physical metaphor shifts.

</Scaffold>

## Further reading

[Other nerds discussing the train tracks](https://puzzling.stackexchange.com/questions/132762/how-many-ikea-train-track-layouts-with-self-intersections)
[3D printable tracks, and trains linked within](https://www.printables.com/model/117903-extended-set-of-wooden-train-track-with-50-unique)
